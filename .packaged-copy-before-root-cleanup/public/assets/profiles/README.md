# Gamer Profile Assets

These player portraits are served by Vite from `/assets/profiles/...`.

Current demo profiles:

- `alex-carter.svg`
- `jordan-lee.svg`
- `sam-rivera.svg`
- `morgan-blake.svg`

The app references these paths in `src/main.jsx` through each customer's `avatarImage` field.
